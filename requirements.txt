faster-whisper==1.0.3
keyboard==0.13.5
pyperclip==1.11.0
sounddevice==0.5.6
# Held on the 1.x line — ctranslate2 / faster-whisper wheels here are built
# against the NumPy 1.x ABI and 2.x has crashed at import. Bump only alongside
# a matching ctranslate2.
numpy==1.26.4
requests==2.34.2
pystray==0.19.5
Pillow==12.3.0
