venv/
__pycache__/
*.pyc
.pytest_cache/
logs/
bench/test_audio.wav

# installer build artefacts (see docs/build-installer.md) — the tracked
# build/ scripts (build.ps1, fetch_model.py) stay in; only outputs are ignored
build/model/
build/dist/
build/work/
installer-output/
